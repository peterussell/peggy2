{
  "name": "rest-server",
  "version": "1.0.0",
  "description": "A REST server for handling requests from the web UI and interfacing with the C++ game server.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Pete Russell",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.14.2",
    "express": "^4.13.3"
  }
}
